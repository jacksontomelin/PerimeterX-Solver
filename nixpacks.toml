# Diz ao Nixpacks para instalar o Python e a biblioteca tls-client automaticamente
[providers.python]
requirements = []

[phases.setup]
pkgs = ["python311", "python311Packages.tls-client"]

# Define o comando correto para iniciar o script do programador
[phases.start]
cmd = "python solve.py"
